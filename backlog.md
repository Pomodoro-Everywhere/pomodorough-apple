# App review backlog

## Fixed - 0.25.0 review AP62-AP71 2026-09-10

- [x] **AP62 Medium - `projectionTransition` capture failure no telemetry + no failure test (AP53 sibling).** Fixed: mirrors `bootstrapRoomState` — `Logger.error` + `SentryCapture.capture` on `captureLocalOperations` throw, advanced phase + message still returned. Test `projectionTransitionReportsCaptureFailureAndKeepsAdvancedPhase` forces a durable-write throw (gated `afterReplacement` hook) and asserts phase advanced, message carried, captured once, store unmutated.
- [x] **AP63 Low - `scheduleIrohStartup` failure Sentry-dark.** Fixed: `Logger.error` + `SentryCapture.capture` before the `.unavailable` status event. Test `scheduleIrohStartupFailureLogsCapturesAndReportsUnavailable` uses the `startError` fixture and asserts the status event + exactly one capture.
- [x] **AP64 Low - join-rollback failure silent + concatenates two localizables.** Fixed: rollback throw logs + captures; join error and rollback state presented as separate sentences (`joinSentence` period-guarded, plain concatenation — no nested `String(localized:)` interpolation). New catalog key `Room join rollback failed: %@` replaces `%@ Room join rollback failed: %@`. Test `controllerJoinAndRollbackFailuresReportSeparateSentencesAndCapture` (fresh join failing activation + `failDelete` vault) pins the `genesis. Room join rollback failed:` boundary, one capture, service stopped, secret retained.
- [x] **AP65 Low - `derivedNextPhase` core failures silent.** Fixed: single `do/catch` over bundle + `selectedPhase` with `Logger.error` + `captureOnce(key:"derived-phase-core")`, nil fallback kept; injectable `coreProvider` (defaults to `SharedCore.bundled()`, all callers unchanged). Test with corrupt-core provider asserts nil + one capture.
- [x] **AP66 Low - corrupt duration settings silently reset to defaults.** Fixed: `contains(key)` check + strict `decodeIfPresent` on present keys; throw logs + `captureOnce(key:"timer-settings-duration-decode")`, then default. Wrong-type fixture test asserts focus falls back to default, siblings intact, double-decode still one capture.
- [x] **AP67 Low - Watch install seed from wall-clock low32 collides.** Fixed: `makeWatchInstallSeed()` (shared, pure, `SystemRandomNumberGenerator`, never 0) replaces the wall-clock seed in `nextSnapshotSequence`. Tests: 256-sample nonzero/variance pin + iOS first-push seed/count shape (`installSeed != 0`, low32 == 1).
- [x] **AP68 Low - stale legacy snapshot (sequence 0) always adopts over newer.** Fixed: 0 adopts only over missing/legacy current, otherwise stale; upgrade-window + downgrade-pin tradeoff documented on `shouldAdoptWatchSnapshot` and the `sequence` field. Existing ordering + pre-sequence tests updated to the new rule; new assertions pin 0-over-nil, 0-over-0, sequenced-over-0.
- [x] **AP69 Low - alarm-cancel path fully silent (`cancelReportsError:false`).** Fixed: `enqueueAlarmOperation` always logs + captures; only user-visible `errorMessage` gated on `reportsError`. Test `alarmCancelFailureWithoutErrorReportStillCapturesAndStaysSilent` (running timer + `adoptRoomWorkspace` to idle with `cancellationError`) asserts one capture, nil `errorMessage`, timer cleared, cancel recorded; existing schedule-failure test still pins the `reportsError:true` UI path.
- [x] **AP70 Low - Watch disk-cache key duplicated as literal.** Fixed: `ingest` uses `storeKey`.
- [x] **AP71 Low - server-fallback strings embed raw enum tokens.** Fixed: token dropped (branch already filters `.rejected`, so it was constant); server `reason` passthrough unchanged. Five new catalog keys (`Server resolved a … action/change.`); stub gains `sync-contract-ack-rejected-silent` (rejected + empty reason) + test asserting the timer-action fallback with no raw token.
- Validation 2026-09-10: `Pomodorough-macOS` build green; full `PomodoroughMacTests` 778 passed / 0 failed / 0 skipped (770 baseline + 8 new: AP62, AP63, AP64, AP65, AP66, AP67-pure, AP69, AP71); `Pomodorough-iOS` (simulator) + `Pomodorough-watchOS` (simulator) builds green; contract ok (341 keys: -6 stale +6 new); audit 0 violations, 6 documented exceptions; `git diff --check` clean. No project.yml change, no pbxproj regen (no new files).

## Fixed - apple 0.24.0 release 2026-09-10

- Release: commit `e62bdb7` ("release apple 0.24.0", MARKETING 0.24.0 build 38, project.yml + regen pbxproj only, mirrors 0.23.0). Tag `v0.24.0` at `e62bdb7`, first attempt green with no preflight failure. Release run `34443919674` all 9 jobs green, published `2026-09-10T06:40:20Z` with 5 assets. All six core-consuming shards logged `CORE_PROVENANCE tag=v0.24.0 commit=cf818b86… sha256=0878d0e7…` (no skew); release notes carry the core line. No core pin introduced — fetch_shared_core.sh resolved latest at build time.

## Fixed - 0.24.0 review AP53/AP55-AP61 2026-09-10

- [x] **AP55 High - bare interpolation broke the contract gate.** `Sources/Views/LongBreakProgressIndicator.swift:16` used a bare interpolated `accessibilityValue`. Fixed: `String(localized:)` with the same wording; key `%arg of 4 toward the next long break, %arg completed today` added to `Resources/Localizable.xcstrings` (341 shipping keys) + `UITests/Fixtures/Localizable.ar-XB.json`. `scripts/check_interface_contract.py` ok.
- [x] **AP53 Medium - `bootstrapRoomState` dropped phase-advance ops silently.** Fixed: `Logger.error` + `SentryCapture.capture` on capture failure, error carried to the caller as a third tuple element (`errorMessage`), advanced phase still returned. `AppStatePersistenceCoordinator.LoadTransition` gains `bootstrapCaptureError` (default nil, non-blocking) surfaced into `AppModel.errorMessage` at restore, mirroring `projectionTransition` → `applyRoomProjection`. Regression test forces a durable-write throw after a phase advance (gated `afterReplacement` hook) and asserts mode kept, phase advanced, error carried + captured once, store unmutated.
- [x] **AP56 Medium - reinstall/seed-wrap bricked Watch snapshots.** Fixed: `shouldAdoptWatchSnapshot` adopts on install-seed (high32) change, so a new epoch — reinstall or UInt32 wall-clock wrap — can never sit behind a numerically larger pre-reinstall sequence. Same-epoch strict-`>` ordering unchanged. Cross-seed unit test (smaller new-epoch sequence adopts; same-epoch stale still rejected).
- [x] **AP57 Low - counter saturation froze Watch snapshots.** Fixed: pure `nextWatchSnapshotSequence(seed:count:)` rolls the install epoch (seed +1, count 1) instead of repeating a saturated sequence the watch rejects as stale; strictly monotonic except the unreachable absolute ceiling (seed and counter both max), where it holds and still reports saturation. Unit test pins rollover order + steady increment.
- [x] **AP58 Low - `finishConfirmedAccountDeletion` 51 lines.** Fixed: tail (durable Offline select + state release) extracted to `completeAccountDeletionOfflineReset() -> Bool`; both functions under the 50-line limit. `audit_code_size.py` 0 violations.
- [x] **AP59 Low - durable-write paths swallowed diagnostics + stale log name.** Fixed: `prepareSignedOutReturnState` log name corrected, `SentryCapture.captureOnce(key:"sign-out-return-state")`, localized user string (new catalogued key `Sign-out paused because its recovery state could not be saved.`). The three extracted-tail write guards log + `captureOnce` per step via `AccountDeletionDurableWriteError` (bare step name for Sentry grouping) with the existing localized retry messages.
- [x] **AP60 Low - logout test shared fixed suites.** Fixed: `failedDetachKeepsAccountUsableThenRetryResets` uses UUID-suffixed scenarios + `LogoutRevocationTestSession` wrapper (drain + invalidate); stub exact matches for the two model scenarios converted to `hasPrefix` (exact strings still match, so existing suites unaffected).
- [x] **AP61 Low - sequence compat claim untested, and actually broken.** The new pre-sequence fixture proved the synthesized decoder throws `keyNotFound` on a missing `sequence` key despite the "defaults 0" comment. Fixed: extension `init(from:)` on `WatchTimerSnapshot` with `decodeIfPresent ?? 0` (memberwise init preserved); test asserts sequence == 0 + adopts over a newer sequenced snapshot.
- Validation 2026-09-10: `Pomodorough-macOS` build green; full `PomodoroughMacTests` 770 passed / 0 failed / 0 skipped (incl. 6 new: AP53 bootstrap, AP56 cross-seed, AP57 rollover, AP61 pre-sequence, plus existing ordering suites); `Pomodorough-iOS` (simulator) + `Pomodorough-watchOS` (simulator, forced `WatchSyncPayload.swift` recompile) builds green; contract ok (341 keys); audit 0 violations, 6 documented exceptions; `git diff --check` clean. New helpers are small pure functions / guard chains, no complexity-threshold risk. No project.yml change, no pbxproj regen (no new files).

## Fixed - account and Watch review 2026-09-10

- All six items from "Open - account and Watch review 2026-09-09" below are fixed and checked. Account fixes (P1 return-state, P2 deletion quarantine, P2 coordinator ordering) landed via the account-fix pass: coordinator reset commits only after credential detachment succeeds, return-state sanitization aborts logout on write failure, Offline mode is persisted/read-back before deletion cleanup, and `RoomReplicationController.completeAccountDeletion` resets mode/generation/quarantine. Watch stale-reply fix: phone-owned monotonic emission sequence (`WatchTimerSnapshot.sequence`, high32 install seed + low32 counter, persisted in injected UserDefaults, stamped in `WatchSyncService.push`); watch `ingest` adopts only strictly newer sequences (`shouldAdoptWatchSnapshot`, legacy 0 always adopts). UI fixes: iOS root minimum height removed (`RootView`), timer landscape branch wrapped in ScrollView (`TimerScreen`), progress indicator announces cycle progress plus daily total (`LongBreakProgressIndicator` + `completedToday` via `TimerMachineCard`).
- Verification 2026-09-10: macOS `WatchCommandIdentityTests` 19/19 green (new `watchSnapshotOrderingAdoptsNewerRejectsStale`, `delayedRefreshReplyCannotResurrectStaleCountdown`); iOS simulator `WatchCommandIdentityTests` 22/22 green (new `watchPushStampsIncreasingSequences` proves increasing stamped sequences through the real reply path); watchOS simulator build succeeds; account suites re-run green post-change (`LogoutRevocationTests`, `RoomReplicationControllerTests`, `AppModelDecompositionTests`, `CentralizedAccountSessionCoordinatorTests`, plus the 6 targeted Iroh/Integration tests); `git diff --check` clean. No commits. Pending: SE landscape runtime rendering, live VoiceOver traversal, cross-transport delayed-delivery repro, storage-fault and account-deletion/background-cycle repros.

## Fixed - apple 0.23.0 release 2026-09-09

- Release: commit `0ade608` ("release apple 0.23.0", MARKETING 0.23.0 build 37, project.yml + regen pbxproj only, mirrors 0.22.0) plus commit `3d7d7f5` (catalog AP51 corrupt-skip error string). Tag `v0.23.0` recreated at `3d7d7f5` after the first tag failed preflight (see below); no release object existed before publish. Release run `34406295739` all 8 jobs green, published `2026-09-09T22:11:10Z` with 5 assets. All three shards logged `CORE_PROVENANCE tag=v0.23.0 commit=238ef9fb… sha256=659a492e…` (no skew); release notes carry the core line. No core pin introduced — fetch_shared_core.sh resolved latest at build time.
- First tag attempt (`0ade608`, run `34403689307`) failed preflight deterministically on "Check interface and localization contract": `PersistedStateCorruptionError.errorDescription` (AP51, commit d7f6e67) returned a bare literal. Fix wraps it in `String(localized:)` (pattern used by every other errorDescription) and catalogs the key in `Resources/Localizable.xcstrings` + `UITests/Fixtures/Localizable.ar-XB.json` (339 shipping keys). Reproduced locally (tag tree exit 1, b0acf3d exit 0); CI on d7f6e67 failed the same step. Not a flake, so no rerun; fixed forward per owner approval.
- Evidence: local `check_interface_contract.py` ok (339 keys), docs/protocol/pins ok, script suite 502 passed / 1 skipped. Other-agent dirty files (AppModel, RoomReplicationController, 3 test files, release workflow, attestation script) verified byte-identical by sha256 before and after; never touched/staged/committed.

## Fixed - 0.23.0 review AP51/AP52/AP54 2026-09-10

- [x] **AP51 High - corrupt op lossy-skip.** `pendingCommands`/`pendingTaskOperations`/`pendingDurationOperations`/`history` now decode via `LossyDecodable` (pattern from autoStart/selectedTask); skips set `hasCorruptPendingOperations`, required queues stay null-strict (`requiredNullState` fixture still throws). `PersistedStateLoader.load` logs + `captureOnce(key:"persisted-state-corrupt-skip")` on newly-detected skips only (total-garbage path still owned by coordinator, no double-capture). 3 tests in `Tests/PersistedStateCorruptSkipTests.swift` (unknown-enum forward-compat + 4-queue corrupt fixture + load-once report).
- [x] **AP52 Medium - revocation replace/remove telemetry.** All five `try?` persist sites route via `persistReplace`/`persistRemove`: `Logger.error` + `captureOnce(key:"logout-revocation-persist")` + write-failure `storageDiagnostic` (read/write counters clear the diagnostic symmetrically, so the existing read-recovery test still passes). 2 tests in `Tests/SessionRevocationPersistTests.swift` (throwing replace retains + diagnoses; throwing remove retains completed + captures).
- [x] **AP54 Low - createRoom rollback capture.** Rollback `secretStore.delete` failure now logs + `SentryCapture.capture` (original error still rethrown; no PII — keychain error only). 2 tests in `Tests/IrohRoomStoreRollbackTests.swift` (failing secret store captures + orphan visible; success path leaves no orphan, no capture).
- [ ] **AP53 Medium - SKIPPED (file dirty by other agent).** `RoomReplicationController.bootstrapRoomState` still does `_ = try? roomStore.captureLocalOperations(from:)` — silent drop of phase-advance ops on launch. Spec for later: mirror `projectionTransition` (same file, returns `.projectionApplied(updated, errorMessage:)` on capture failure) — surface the error via `Logger.error` + `SentryCapture.capture` and carry it to the caller (return `(mode, state, errorMessage?)` or equivalent), plus a regression test that forces `captureLocalOperations` to throw after a phase advance and asserts the error is reported and the advanced phase kept. Implement only on a clean tree; never revert the in-flight `completeAccountDeletion` hunk.
- Validation: `Pomodorough-macOS` build green, full `PomodoroughMacTests` 1040 passed / 0 failed / 0 skipped (incl. 7 new). `audit_code_size.py`: 1 violation, all in other-agent dirty `Sources/AppModel.swift:1065` (untouched). Complexity report: no Apple regressions from these changes. Other-agent dirty files (`AppModel.swift`, `RoomReplicationController.swift`, 3 test files, release workflow, attestation script) never touched/reverted/staged; `project.pbxproj` regen contains only the 3 new test files.

## Open - account and Watch review 2026-09-09

- Review evidence: current-source inspection of account logout/deletion, room recovery, Watch snapshot ingestion, and SwiftUI layout/accessibility. Six new findings below include triggers and fix directions. No builds, automated tests, GUI sessions, account deletion, or storage fault injection ran in this review. Prior green suites are historical, not verification of these cases. Only backlog entries were added; concurrent work elsewhere in this file remains untouched.

- [x] **P2 - Late Watch refresh replies can overwrite a newer snapshot.** `Watch/WatchSyncService.swift:35-58,187-214,229-234` sends refresh replies and application-context updates into the same unconditional `ingest`. No snapshot revision/sequence comparison prevents an older reply from replacing and persisting newer state. Reproduce by delaying a running-timer refresh reply, delivering a newer paused/completed context, then releasing the reply. The Watch returns to the old countdown/buttons, including after an offline relaunch; timer controls then fail their phone-side revision checks. Add an ordering identity owned by the phone with a defined session/reset policy, and reject older snapshots across both delivery paths. Do not rely only on wall-clock dates, which can move backward. Source-confirmed; cross-transport delayed-delivery reproduction pending.

- [x] **P2 - iOS root minimum height exceeds small-phone landscape height.** `Sources/Views/RootView.swift:23-24` imposes a 420-point minimum on the entire tab/navigation hierarchy, but an iPhone SE landscape screen is only 375 points high before safe areas. Content is forced beyond the available viewport; the timer's landscape branch has no ScrollView recovery (`Sources/Views/TimerScreen.swift:17-26`). Remove the iOS root minimum and fit layout to actual available space. Verify SE landscape in Timer, Tasks, and Account, including keyboard and accessibility sizes. Source-confirmed sizing mismatch; exact clipped controls and runtime rendering pending.

- [x] **P3 - VoiceOver calls long-break cycle progress a daily total.** `Sources/Views/LongBreakProgressIndicator.swift:14-15` announces "N of 4 pomodoros today", but `Sources/AppModel.swift:581-582` supplies cyclic long-break progress. After the fifth completed focus session, it announces "1 of 4 pomodoros today" rather than explaining progress toward the next long break. Label the value as cycle progress; expose the actual daily total separately if needed. Source-confirmed accessible-string mismatch; live VoiceOver traversal pending.

- [x] **P1 - Failed room return-state cleanup can restore signed-out account data.** `Sources/AppModel.swift:945-954` catches `replaceActiveReturnState` failure but still completes signed-out persistence. The old room return workspace remains available. Reproduce by injecting a pre-write failure for that replacement while signing out inside an Iroh room, restoring storage, then leaving the room. Previous account history and cached user can reappear and be persisted; authentication tokens are not restored. Make cleanup durable or retain a cleanup obligation that blocks restoration until sanitization succeeds. Source-confirmed; storage-failure reproduction pending.

- [x] **P2 - Successful account deletion leaves Iroh mode and deletion quarantine active.** `Sources/AppModel.swift:1053-1092` purges rooms and clears recovery state without resetting replication mode or finalizing `RoomReplicationController` quarantine. Reproduce by deleting an account in Iroh mode, then starting a timer or switching Offline without relaunching. The selected mode has no active room, so persistence/leave fails. A newly created room can also stop reconnecting after background/foreground because endpoint startup remains quarantined. Finalize successful deletion by durably selecting Offline, clearing room presentation, and releasing quarantine after cleanup. Source-confirmed; account-deletion and background-cycle reproduction pending.

- [x] **P2 - Failed credential detachment leaves session coordinator signed out while UI remains signed in.** `Sources/AppModel.swift:895-918` begins coordinator reset before awaiting logout and returns on failure without rollback. `CentralizedAccountSessionCoordinator.applyReset` has already changed publication and invalidated verification/sync ownership, while failure to append a logout obligation leaves credentials intact. Reproduce by making revocation-store append throw during Sign out, then attempting sync. UI initially retains the account but coordinator rejects synchronization; later publication can show local-only without signed-out cleanup. Account-switch cancellation at `AppModel.swift:876-887` has the same ordering. Prepare then commit reset after durable detachment, or restore the prior session lifecycle on failure. Source-confirmed; injected-failure reproduction pending.

## Fixed - shared-core version pin removed 2026-09-09

- Owner killed the pin precedent: Apple always builds against the latest Core release, never a hardcoded commit. `scripts/fetch_shared_core.sh` (fail-closed `set -euo pipefail`) resolves the latest non-prerelease `pomodorough-core` release at build time via the `releases/latest` API, gates tag/commit/sha formats (strict SemVer, 40-hex, 64-hex), downloads `pomodorough_core.wasm` + `SHA256SUMS` with `gh release download`, cross-checks the manifest, verifies the Sigstore attestation bound to the core release workflow (`--signer-workflow`, `--source-digest`, `--source-ref`), then writes `Resources/SharedCore/pomodorough_core.wasm` + `CORE_RELEASE_TAG`/`CORE_COMMIT`/`CORE_SHA256` and prints `CORE_PROVENANCE` (also to the step summary).
- CI and all six core-consuming Release jobs (`preflight`, `test-ios`, `test-macos`, `build-ios-simulator`, `build-ios-device`, `build-macos`) call the script with `GH_TOKEN`; no `CORE_*` env pins, no pinned checkout, no Rust rebuild-from-source left in either workflow. Built-app checks compare bundled wasm against the build-time `CORE_SHA256` file. `package-and-release` downloads the three shards' provenance artifacts, fails closed on any shard skew (`cmp` on tag/sha), verifies all three staged apps carry the agreed wasm, records provenance to `core-provenance.env`, and stamps the core tag/commit into the release notes.
- Deleted the pin surface: `SharedCore.coreCommit`/`coreSHA256` + runtime hardcoded-digest check (`CryptoKit` import gone; structural export checks remain), the exact-`0.22.0` dispatch assertion (now schema 1 + SemVer shape), the corrupt-module test (now corrupts the magic header deterministically), and orphaned `scripts/verify_shared_core_provenance.py`.
- Guard tests rewritten to the new contract (`scripts/tests/test_shared_core_workflow.py`): zero retired pin literals across workflows/scripts/Sources/Tests/`project.yml` (literals split so the guard stays self-clean), workflows resolve latest at build time, fetch script attests + records provenance, built apps check against resolved provenance, release shards agree. `WORKFLOW_PREFIX_SHA256` in `test_completed_native_log_capture.py` re-pinned after reviewing the exact CI prefix diff (permission + pin removal + fetch step only; retained job untouched).
- Evidence: local fetch resolves `v0.22.0` (`a8bcd1553239821a892dd2fab029b2aa2e8f3a08`, `f119e7d3…`, attestation + manifest OK). Full `PomodoroughMacTests` 754 passed / 0 failed / 0 skipped (1,028 executions). Script suites 498 green (shared-core 8, release-workflow 4, interface/compat 14, package-regen 42, native-log 43, xcode-runner 387 + 1 skip). All schemes build (macOS, iOS sim + Release device, watchOS sim, both PersistedStateCompatibility). Contract checks ok (action pins, 338 interface keys, docs, protocol fixture); `audit_code_size.py` 0 violations; complexity report run with no Apple regressions.
- Release: commit `b0acf3d` ("Remove shared-core version pin; resolve latest Core release at build time"), tag `v0.22.1` (MARKETING 0.22.1, build 36), Release run `34351945570` all 8 jobs green, published `2026-09-09T13:15:43Z` with 5 assets. All six shards logged `CORE_PROVENANCE tag=v0.22.0 commit=a8bcd155… sha256=f119e7d3…` (no skew); release notes carry the core line. Standalone CI push runs fail on the hosted-runner `sandbox-exec: sandbox_apply: Operation not permitted` SwiftPM flake in the CI-only "Test iOS with bounded finalization" step (`run_xcode_tests.py test-without-building`): v0.22.0 pre-change run, both v0.22.1 runs, and a failed-job rerun all fail identically there. Untouched code path (no diff in the test runner, SwiftPM resolution, caches, or xcodebuild flags); release-pipeline iOS/macOS tests on the same commit are green, as are local builds and the full local macOS suite.

- [x] **AP42 High - reachable refresh served stale replySnapshot.** `push()` ran only on local mutations, so room swaps, completions, bootstrap resolutions, and sign-outs left the reply cache stale. Fixed: every projection refreshes the cache (`installProjection` observation hook + `installUnreducedBaseSnapshot`), with explicit `push()` in `adoptRoomWorkspace`, iroh completion, bootstrap submission, and sign-out/quarantine paths; the refresh delegate schedules a converging `push()` after serving. Covered by `roomSwapRefreshReplyConvergesWithoutLocalMutation` (iOS): room swap with no local mutation, refresh reply matches `makeWatchSnapshot()` identity/revision/status. Negative control (pushes disabled) fails as expected.
- [x] **AP43 Medium - concurrent handleIncoming Tasks unordered.** Fixed: phone-side application chained behind `WatchCommandSerialQueue` (shared, Sendable, tail-chained MainActor Tasks), so pause-then-resume applies in arrival order even across suspension. Covered by `serialQueueAppliesPauseThenResumeInOrder` (AppModel pause/resume with a hop, asserts running) and `serialQueuePreservesArrivalOrderAcrossSuspension` (order log across `Task.yield`).
- [x] **AP44 Medium - ungrounded tap dropped silently.** Fixed: nil stamp now sets localized `commandError` ("This timer changed on your iPhone. Refreshing status."), `logOnce(key:"watch-command-ungrounded")`, and `requestSync()`. Covered by `ungroundedControlFails` plan test + alert contract test hitting the new key.
- [x] **AP45 Medium - mixed-version interop.** Documented in the release-gate section below; no compat code beyond defaulted decoders + fail-closed (guessing intent cross-version risks wrong-timer mutation, not trivially safe).
- [x] **AP46 Low - commandError never cleared.** Fixed: cleared on live-send attempt, queued delivery (including send-error fallback), and in `ingest`. Covered by `successfulPlansClearCommandError` + contract assertion that the watch source clears in all three places.
- [x] **AP47 Low - unknown command fail-open.** `applyWatchCommand` default now returns false with `Logger.error` (name only) + `SentryCapture.captureOnce(key:"watch-command-unknown")`. Covered by `unknownWatchCommandRejectedAndCaptured` (state unchanged, capture holds the bare name).
- [x] **AP48 Low - alert strings unlocalized.** All four watch alert strings (plus the new ungrounded one) use `String(localized:)`; entries added to `Resources/Localizable.xcstrings` (338 keys, byte-identical to generator output) and the ar-XB pseudo fixture. Covered by `WatchAlertContractTests` (localized API, shipping + pseudo parity with RTL decoration).
- [x] **AP49 Low - silent sync failures.** iOS logs + `captureOnce(key:"watch-snapshot-unavailable")` when the reply cache is nil; watch `logOnce(key:"watch-reply-missing-snapshot")` on a missing reply key.
- [x] **AP50 Low - watch decision logic untested.** Stamping + send + send-error decisions extracted to pure functions in shared `Sources/WatchSyncPayload.swift`, covered by 9 `WatchSendDecisionTests` in the macOS/iOS bundle (nil snapshot, id mismatch, Start live-only x reachable/unreachable, error fallback, error-clear semantics).

### Fix verification - 2026-09-09

- Full `PomodoroughMacTests` suite passed: 754 tests, zero failures/skips (`/tmp/watchfix-mac3.xcresult`). Includes 14 new tests (9 decision, 3 contract, 2 identity). Note: two earlier full runs each showed one failure in pre-existing `deletingTaskUsesProjectedSelectionOverStoredSelection` (uncommitted prior-session work in `SynchronizedWorkspaceMutationController.swift`/`CoverageGapBehaviorTests.swift`, untouched by this fix); it passes in isolation (5/5) and in the final full run, so it is a parallel-load flake, not a regression.
- iOS Simulator (Pomo-iPhone): `WatchCommandIdentityTests` 19/19 pass, plus decision (9) and contract (3) suites green — 31/31 total (`/tmp/watchfix-ios-final.xcresult`). AP42 negative control verified red with pushes disabled.
- Builds: `Pomodorough-watchOS` device/simulator build succeeds; `Pomodorough-iOS` and `Pomodorough-macOS` build-for-testing succeed. `check_interface_contract.py` ok (338 keys); `audit_code_size.py` 0 violations.
- Device-pending (no watchOS test host, no paired hardware): real-radio ordering/latency, multi-Watch reactivation, live VoiceOver, and on-watch alert rendering.

## Fixed - task-selection re-review 2026-09-09

- Validation: iOS Simulator synchronized-mutation suite passed all 7 tests after the fix. Full `Pomodorough-macOS` test command passed. Existing `SentryCaptureTests.swift:927` actor-isolation warning remains. `git diff --check` passed. This pass inspected synchronized task mutations, logout revocation, alarm planning, Watch payloads, and task/history-resolution views; it did not repeat whole-app GUI, physical paired-Watch, or multi-peer network testing. Concurrent edits elsewhere were preserved.

- [x] **P2 - Deleting a stale base selection clears the newer visible task selection.** During sync, stored selection A can coexist with pending visible selection B. `SynchronizedWorkspaceMutationController.planTask` compared deletion against stored A, so deleting A appended a newer Unassigned operation and erased B; deleting visible B omitted the explicit selection-clear operation. Changed the comparison to `snapshot.projectedSelectedTaskID`. Regression constructs a real pending selection operation over a stale base and tests both deletion branches. Before the fix, three assertions failed; afterward all seven synchronized-mutation tests passed on iOS Simulator.

## Release gate - watch/phone same-train shipment (AP45) - 2026-09-09

- Mixed-version interop is fail-closed, not deadlocked. New phone + old Watch: timer controls without revision fail closed (`legacyPauseWithoutRevisionRejected`); settings commands still apply; deferred Start stays rejected live-only. Old phone + new Watch: unknown JSON keys decode past, pre-revision apply behavior. Either direction converges on the next snapshot push; no retry loop exists on either side.
- No further compat code: auto-applying revision-stamped commands on an old phone, or guessing intent for unrevisioned controls on a new phone, risks wrong-timer mutation and is not trivially safe. Documented instead of implemented.
- Same-train gate for 0.22.0: ship iOS and watchOS together; do not release one side alone. Both sides must carry the revision work before timer controls are trusted cross-device.

## Incorporate watch command revision work - 2026-09-09

- WIP implements `WatchTimerRevision` compare-and-set, Start live-only deferred rules, reply-snapshot cache, `commandError` UI. New fields defaulted so legacy payloads decode; legacy controls without revision fail closed (covered by `legacyPauseWithoutRevisionRejected`).
- Incorporation fix: split `Watch/WatchSyncService.send` (63 lines) into `send` + `stampedTimerControl` + `deliver`; `audit_code_size.py` now 0 violations.
- Evidence: macOS full suite 739 tests pass (`/tmp/watch-full-mac.xcresult`); iOS `WatchCommandIdentityTests` 16 pass (`/tmp/watch-identity-ios.xcresult`); related macOS `ResponsibilitySplit`+`IrohReplication` 67 pass; watchOS + iOS Simulator builds succeed.

## Fixed - follow-up review 2026-09-09

- [x] **P2 - Delayed or replayed Watch Start can create an unwanted timer after newer phone activity.** Previously Start used the offline/fallback queue without a precondition, so delivery after a newer phone timer ended could create an unsolicited timer. Fixed by making Start live-only: the Watch never queues it, including a reachability race or live-send failure, and shows explicit failure guidance. The iPhone rejects Start received through either deferred transport, including transfers queued by older Watch builds. Pause/Resume/Finish retain revision-checked queued delivery. Regression covers rejected deferred Start and queued replay after a completed timer. Live Start is not a durable exactly-once protocol; this fix removes automatic deferred delivery/retry. Physical transport reproduction remains pending.

- [x] **P2 - Reachable Watch refresh receives an acknowledgement instead of the snapshot it expects.** Previously the Watch expected `reply[WatchSyncKeys.snapshot]`, but the iPhone returned only `report: "ack"`, leaving refresh dependent on background context delivery. Fixed: attachment and each publication encode the latest snapshot on the main actor into a mutex-protected cache, even before connectivity activation. The refresh delegate returns that snapshot directly without transferring WatchConnectivity's non-Sendable callback between actors. iOS Simulator regression invokes the actual delegate and verifies running and then paused snapshots, including current revisions. Paired-device latency remains unverified.

### Follow-up fix verification

- iOS Simulator: all 16 `WatchCommandIdentityTests` passed, zero failures/skips. Result: `~/Library/Developer/XcodeBuildMCP/workspaces/apple-ae1980e0bbd6/result-bundles/test_sim_2026-09-09T06-03-27-685Z_pid37497_9fd37dd3.xcresult`. The initial macOS targeted run passed 15 tests; final reply-cache/delegate validation ran on iOS.
- iOS Debug and watchOS Simulator builds passed. Existing `SentryCaptureTests.swift:927` actor-isolation warning remains unrelated. Full suites were not rerun. No physical device installation or paired-radio testing in this fix pass; Watch error-alert rendering remains device/simulator-UI pending.

### Pre-fix follow-up coverage

- Reviewed current Watch controls, cached snapshots, live/queued transport, receiver command acceptance, and timer Start planning. Existing fixes and historical entries remain unchanged. This pass changes only this backlog; no application fixes or commits.
- Findings above are source-confirmed, not new runtime reproductions. No builds or tests rerun in this follow-up. Earlier successful test/build results remain historical evidence, not coverage of these cases. Paired Watch delivery, live accessibility, real alarms, and broader app flows still need validation. Delegated review was unavailable because its usage limit was reached; this pass used direct source inspection.

## Fixed - 1.0 readiness check 2026-09-09

- [x] **P2 - Restoring a running local timer after a terminal room timer skips its alarm.** The old terminal-timer early return skipped scheduling when leaving a completed/cancelled room timer for a still-running local timer. Fixed in `Sources/TimerSessionController.swift`: nil and terminal predecessors now schedule an incoming owned running timer. Regression tests cover ownership, terminal statuses, inactive destinations, remaining duration, and actual room workspace restoration through AppModel. End-to-end OS alarm delivery remains device-pending.

- [x] **P2 - Delayed Watch commands can override newer actions on the same timer.** Fixed with `WatchTimerRevision` in the shared payload. Snapshots carry timer identity, last intent identity, and anchored timer state; Watch controls echo that revision through live and queued delivery. AppModel accepts Pause/Resume/Finish only against the current revision and valid status. Successful mutations invalidate replays, including after relaunch, without comparing clocks or storing an unbounded command-ID set. Regression tests cover stale same-timer controls, live/queued reordering at the receiver, replay, relaunch, payload round-trip, fresh controls, and previous-timer rejection. Legacy payloads still decode, but controls without revision data fail closed; both phone and Watch must be updated for these controls. Physical WatchConnectivity delivery remains device-pending.

### Fix verification - 2026-09-09

- Full `Pomodorough-macOS` suite passed: 737 tests, zero failures or skips, 1,011 executions including parameters. Result bundle: `/var/folders/r_/_mr22dqn24d31b7460cz8z5m0000gn/T/opencode/readiness-fixes-full.xcresult`. After strengthening two previous-timer assertions, the Watch-command suite passed again in `readiness-fixes-watch-tests.xcresult` in the same directory.
- iOS and watchOS Release simulator builds passed with these source fixes. Device-level release checks below remain pending; these fixes do not constitute a device release sign-off.

### Validation - 1.0 readiness

- Latest `Pomodorough-macOS` test run passed: 731 tests, zero failures, zero skips; 994 executions including parameterized cases. Result bundle: `/var/folders/r_/_mr22dqn24d31b7460cz8z5m0000gn/T/opencode/release-readiness-mac-20260909.xcresult`.
- Latest iOS Debug build/install/launch passed on iPhone SE 2nd generation / iOS 27 Simulator. iOS Release simulator build and watchOS Release simulator build also passed. These are simulator builds, not signed device archives.
- Rechecked the small-phone picker with an unassigned idle and running focus timer at default text size. The active layout now stacks, and text stays inside its panel. Starting with alerts disabled still shows an explicit warning; cancelling returns to idle. No review timer remains running.
- Dark Mode room-name retest reached the field, but text injection failed; the fallback mobile automation agent download timed out. Entered-text contrast remains unverified in this pass. Long task names, break layouts, live VoiceOver, largest Watch text, paired Watch delivery, real-device alarms, authenticated multi-device sync, and fresh-install/upgrade Release smoke tests remain pending. A connected iPhone was discovered but was not modified.
- Pre-fix review recommendation was to hold 1.0 for the two correctness cases above and device-level validation. Both code cases now have fixes and regression coverage as recorded above; device validation remains outstanding.

## Open - review 2026-09-09

- [x] **P1 - Joining a room can discard local timer actions accepted during connection.** `Sources/RoomReplicationController.swift:286-295` captures the return workspace before awaiting endpoint startup and handshake, then activates the room with that old snapshot. While joining, automatic completion is suspended but timer mutations remain enabled, including macOS menu-bar Start/Pause (`Sources/AppModel.swift:562-567,1933-1938`; `Sources/Views/MenuBarTimer.swift:107-119`). Start or pause during a slow join, complete the join, then leave: the pre-join snapshot is restored and persisted (`Sources/IrohRoomStore.swift:418,477-482`; `Sources/AppModel.swift:1981-1986`), losing the accepted action. Capture the current workspace immediately before activation or block mutations throughout the transition. Add a gated-handshake test with an intervening mutation and leave/relaunch verification. Source-confirmed; runtime race reproduction pending. Fixed — working tree (this session): `joinRoom` re-reads `dependencies.workspaceSnapshot().state` immediately before activation and passes the fresh snapshot to `activateJoinedRoom` (`Sources/RoomReplicationController.swift`); covered by `controllerCapturesReturnAfterTransportAndLeavesCorrectWorkspace`, full `PomodoroughMacTests` green.

- [x] **P2 - Start can assign a different task than the picker displays during sync.** `Sources/SynchronizedWorkspaceMutationController.swift:455-458` reads base `state.selectedTaskID` instead of `snapshot.projectedSelectedTaskID`. A response from an in-flight sync can install old server selection A while pending selection B remains projected in the UI (`Sources/AccountSynchronization.swift:393`; `Sources/AppStatePublisher.swift:51`; `Sources/AppModel.swift:2314`). Select B during sync, let that response arrive, delay/fail the follow-up upload, then Start: the focus run is assigned to A or left unassigned despite B being displayed. Use projected selection when constructing Start, and test with a gated sync response. Source-confirmed; network-timing reproduction pending. Fixed — working tree (this session): `selectedTaskID(for:snapshot:)` reads `snapshot.projectedSelectedTaskID` (`Sources/SynchronizedWorkspaceMutationController.swift`); covered by `startTimerUsesProjectedSelectedTaskOverStoredSelection`, full `PomodoroughMacTests` green.

- [x] **P2 - Leaving or switching rooms does not reconcile system timer alarms.** `Sources/AppModel.swift:1981-1986,1856-1860,1943-1946` replaces the workspace and rebuilds its projection without reconciling alarms. Start a room timer, wait for scheduling, then leave to an idle local workspace: the departed timer's alarm remains scheduled even though the app no longer tracks that timer. Generic publication (`Sources/AppStatePublisher.swift:60-73`) handles tracked completion alerts, not outgoing running alarms. Capture the outgoing timer and reconcile alarms after successful workspace changes, including scheduling an incoming owned running timer. Add running-room leave/switch tests that assert alarm cancellation and scheduling. Source-confirmed; OS alert reproduction pending. Fixed — working tree (this session): new `adoptRoomWorkspace(_:)` captures the outgoing timer and calls `reconcileAlarm(from:to:at:)` after projection, wired into set/create/join/leave room paths (`Sources/AppModel.swift`); covered by `leavingRoomCancelsDepartedRunningTimerAlarm`, full `PomodoroughMacTests` green.

- [x] **P2 - Remote resume schedules an alarm on non-owner devices.** `Sources/TimerSessionController.swift:456-467` schedules any changed running timer without checking `ownsCurrentTimer`, unlike the initial/new-timer branches at `:431,447`. Let device A own a timer and device B observe it; pause and resume on A. When B receives the resume, it schedules its own alarm, so devices that correctly stayed silent at initial Start can ring later. Apply the ownership check to same-timer corrections while retaining cancellation of obsolete alarms. Parameterize status/elapsed correction tests by ownership. Source-confirmed; multi-device reproduction pending. Fixed — working tree (this session): same-timer correction branch now requires `ownsCurrentTimer` for scheduling while always cancelling the obsolete alarm (`Sources/TimerSessionController.swift`); covered by parameterized `sameTimerCorrectionOnlySchedulesWhenOwned`, full `PomodoroughMacTests` green.

- [x] **P2 - Ordinary-size task rows expose counts without accessible meaning.** `Sources/Views/TaskSummaryRow.swift:30-39` exposes a bare completed count and compact duration as separate text elements; `.contain` at `:18` supplies no metric labels, and `Sources/Views/TaskBoardHeader.swift:23` hides the column headings from accessibility. VoiceOver navigation at ordinary text sizes encounters numbers such as "3" without "finished pomodoros". The larger-text branch already includes that context. Combine only the read-only summary with labelled count and `TaskTimeText.spoken` duration, retaining the independently accessible trash button. Source-confirmed; live VoiceOver traversal pending. This is separate from the resolved delete-button reachability issue. Fixed — working tree (this session): ordinary-size summary content is one combined element labelled with count plus `TaskTimeText.spoken` duration, trash button stays independent (`Sources/Views/TaskSummaryRow.swift`). Source-confirmed; live VoiceOver traversal pending.

- [x] **P2 - Watch countdown ignores larger text preferences.** `Watch/WatchTimerView.swift:79-84` fixes the essential countdown at 58 points and allows shrinking to 29 points, without Dynamic Type scaling. Increasing Watch text size does not enlarge the primary timer readout; long durations on smaller Watches can shrink it instead. Use a scaled metric or semantic text style with a layout that accommodates larger text. Source-confirmed; small-Watch and largest-text rendering pending. This is distinct from the resolved iOS dial sizing issue. Fixed — working tree (this session): countdown uses `@ScaledMetric(relativeTo: .body)` base 58 (`Watch/WatchTimerView.swift`). Source-confirmed; small-Watch and largest-text rendering pending.

- [x] **P2 - Room-name input becomes nearly unreadable in Dark Mode.** `Sources/Views/NetworkSectionView.swift:248-250` uses fixed near-black `PomodoroughTheme.track` text on a system-adaptive rounded text field. Runtime-confirmed on iPhone SE 2nd generation / iOS 27 Simulator: open Account > Network, enable Dark Mode, and type "Review room"; the entered text is nearly invisible against the black field. The replacement-room field at `:206-208` uses the same styling. Use `.primary` for native fields or explicitly pair foreground and background colors. Verify both normal and replacement-room fields in both appearances. Normal room-name field reproduced; replacement-room and macOS rendering remain source-only. Fixed — working tree (this session): both room-name fields use `.primary` (`Sources/Views/NetworkSectionView.swift`); both appearances still need a device check.

- [x] **P2 - Active next-task picker overflows its panel on a small iPhone at default text size.** `Sources/Views/TimerTaskPicker.swift:12-20,31-71,90-93` retains a three-column horizontal layout outside accessibility sizes and only requests intrinsic vertical sizing for accessibility sizes. Runtime-confirmed on iPhone SE 2nd generation / iOS 27 Simulator: start an unassigned focus timer and scroll to its controls. The next-task value breaks into "Unas-" / "signed", with the second line below the dark task panel; the focus-task heading is also squeezed into two lines. Stack current/next task controls when available width is insufficient and size the panel to the rendered menu label. Verify idle, active focus, and break states with short and long task names. Related to the older resolved accessibility-size picker issue, but reproducible in the current build at default text size. Fixed — working tree (this session): standard sizes use `ViewThatFits` horizontal `HStack` with a stacked fallback and intrinsic panel height (`Sources/Views/TimerTaskPicker.swift`); device check of idle/active/break states with short and long names pending.

### Validation - 2026-09-09

- Reviewed current working-tree timer, synchronization, room-transition, task accessibility, and Watch UI code, including uncommitted fixes. Added eight findings: one P1 and seven P2. Existing backlog entries and unrelated changes are preserved. No application code changed by this review.
- Built `Pomodorough-iOS` successfully, installed the fresh build, and inspected iPhone SE 2nd generation / iOS 27 Simulator. Checked Pattern, Account, Network in light/dark appearances, and timer Start/Pause/Cancel with alerts disabled. Reproduced the dark room-name field and active next-task panel overflow. Cancelled the review timer and restored light appearance afterward.
- Six other findings are source-confirmed, not runtime-reproduced. Automated tests were not rerun; earlier test totals below belong to earlier work. macOS runtime, paired Watch, live VoiceOver/Voice Control, authenticated sync, multi-peer races, and actual OS alarm delivery remain unverified. Simulator accessibility inspection was unavailable because of the automation sandbox; UI checks used screenshot-grounded coordinate interactions. This review is not exhaustive platform or flow coverage.

## Open - review 2026-09-08

- [x] **P1 - Reachable Watch commands do not reach the iPhone handler.** `Watch/WatchSyncService.swift:105-114` sends commands with `replyHandler: nil`, but `Sources/WatchSyncService.swift:94-103` implements only the message delegate overload with a reply handler. Start, Pause, Resume, Done and settings commands therefore have no matching receiver while the phone is reachable. The send error handler only logs; it does not queue the command as its comment claims. Implement the no-reply delegate and route it through `handleIncoming`, or use an acknowledged request/reply command path. Add delivery tests, not only error-logging tests. Source-confirmed; paired-device reproduction pending. Fixed — working tree (this session): added `session(_:didReceiveMessage:)` no-reply overload routing through `handleIncoming` (`Sources/WatchSyncService.swift`); send-failure now falls back to ordered `transferUserInfo` (`Watch/WatchSyncService.swift`); covered by `Tests/WatchCommandIdentityTests.swift`, full `PomodoroughMacTests` 728 green.

- [x] **P2 - Offline Watch refresh replaces queued commands.** `Watch/WatchSyncService.swift:118` stores a command in application context, while `:80-82` writes a sync request to the same replaceable context. Repro: issue Pause offline, then reopen the Watch screen before delivery; its appearance requests a sync and replaces Pause. Multiple offline commands also replace each other. Use an ordered, acknowledged queue for commands and reserve application context for replaceable state, or disable offline mutations with explicit feedback. Source-confirmed from sender and appearance/reachability paths; transport reproduction pending. Fixed — working tree (this session): offline and failed sends go via `transferUserInfo` (one ordered transfer per command); application context stays reserved for replaceable snapshot pulls; iOS handles `didReceiveUserInfo` (`Sources/WatchSyncService.swift`). No watchOS test host exists, so transport stays device-pending; payload/ordering decisions covered by `Tests/WatchCommandIdentityTests.swift`.

- [x] **P2 - Delayed Watch actions can change the wrong timer.** `Sources/WatchSyncPayload.swift:52-59` carries no target timer ID, and `Sources/AppModel.swift:650-655` applies incoming actions to the current timer without checking their age. Repro: queue Pause or Done for timer A while offline; finish A and start B on the phone before delivery. The delayed action then pauses or completes B. Include timer and command identities, reject stale/mismatched operations, and return a fresh snapshot after rejection. Source-confirmed; delayed-delivery reproduction pending. Fixed — working tree (this session): `WatchTimerCommand` carries `id` + `timerId`, `WatchTimerSnapshot` carries `timerId` (both back-compatible decoders); `applyWatchCommand` returns `false` for stale timer-targeted commands and `handleIncoming` pushes a fresh snapshot on rejection; watch sends its snapshot's `timerId` on Pause/Resume/Done. Covered by `Tests/WatchCommandIdentityTests.swift` (stale pause/finish rejected, current/legacy accepted).

- [x] **P2 - Switching paired Watches leaves connectivity deactivated.** `Sources/WatchSyncService.swift:108` leaves `sessionDidDeactivate` empty. After switching the active Watch, snapshot pushes fail the `.activated` guard at `:32-35`; `sessionWatchStateDidChange` only attempts another push. Reactivate the session in `sessionDidDeactivate` and push after successful activation. Source-confirmed; multi-Watch device validation pending. Fixed — working tree (this session): `sessionDidDeactivate` calls `session.activate()`; the existing `activationDidComplete` handler pushes the fresh snapshot. WCSession cannot be driven without a watchOS test host, so multi-Watch validation stays device-pending.

- [x] **P2 - Duration and task-delete buttons remain hidden from accessibility.** `Sources/Views/DurationRow.swift:28-52` replaces the visible phase and plus/minus controls with one adjustable button, hiding the individually labelled buttons at `:81,87`. `Sources/Views/TaskSummaryRow.swift:18-22` likewise replaces the visible trash button with text and a custom Delete task action. Voice Control cannot target these visible controls by their button labels; VoiceOver users must discover adjustment gestures or custom actions. Preserve independently accessible duration and delete buttons, with matching labels; combine only read-only row content. This remains after the earlier TimerControls/TaskComposer fix. Source-confirmed; live Voice Control validation pending because simulator accessibility inspection is unavailable. Fixed — working tree (this session): removed both `.accessibilityRepresentation` wrappers; phase button exposes label + value + hint, steppers keep "Reduce/Increase {phase} duration" labels, delete keeps "Delete {task}" via `.accessibilityElement(children: .contain)`; `UITests/PomodoroughAccessibilityUITests.swift` now asserts each control is individually reachable (phase controls helper, direct trash-button tap). Live Voice Control validation still pending.

### Validation - 2026-09-08

- Review-only changes to this backlog; no application fixes. Inspected timer, Watch transport, task, history, account, network and accessibility paths. Findings above distinguish source evidence from pending device reproduction.
- iOS Simulator build and launch succeeded on iPhone SE 2nd generation / iOS 27. Visually checked permission introduction, scrolling to its actions, Not now dismissal, idle timer and Pattern navigation at default text size. This pass did not rerun automated tests or validate macOS, paired Watches, live VoiceOver/Voice Control, authentication or multi-peer sync.
- RocketSim semantic inspection failed with `accessibility_unavailable` / `remote_automation_unavailable`; used screenshots and coordinate input for the limited UI checks instead. Historical test results below belong to earlier reviews, not this pass.

### Fix validation - 2026-09-08 (code fixes, uncommitted working tree)

- macOS build + watchOS build succeed. Full `PomodoroughMacTests` suite passed: 728 tests in 47 suites (includes new `Tests/WatchCommandIdentityTests.swift`, 10 tests: identity round-trip, legacy payload/snapshot decode, snapshot timerId, stale pause/finish rejected, current/legacy accepted, settings unaffected).
- iOS `build-for-testing` on generic simulator succeeds (compiles app + `PomodoroughAccessibilityUITests` with the individual-reachability assertions).
- Code-size audit: 0 violations, 6 documented exceptions. Complexity: apple mean 3.03→3.02; new `applyWatchCommand` guard branches land at cyclomatic 15 (under threshold; branches required by the stale-command finding).
- `git diff --check` clean on touched paths. Unrelated working-tree changes preserved untouched (`install-real-17.sh`, `scripts/run_xcode_tests.py`); the `Watch/WatchTimerView.swift` redesign (other agent) was verified to carry the new `timerId` sends on Pause/Resume/Done.
- Still device-pending (no watchOS test host in project): paired-device no-reply delivery, offline queue ordering over real transport, multi-Watch reactivation, live Voice Control targeting.

## Resolved — review 2026-09-06 (fixed and verified 2026-09-07)

- [x] **P2 — Small task-composer heading uses low-contrast text in light appearance.** `Sources/Views/TaskComposer.swift:13-16,27` renders the caption-sized “ADD TASK” in `PomodoroughTheme.signal` on the system background. `Sources/PomodoroughTheme.swift:6` hard-codes that foreground to `#FF604F`, yielding approximately 2.98:1 contrast against white. Small text becomes difficult to read for low-vision users. Use an adaptive, darker foreground for text on light surfaces and verify both appearances and Increase Contrast; retain the accent separately for decoration. Source/color-value confirmed.

- [x] **P2 — Task selection clips even “Unassigned” at the largest accessibility text size.** Runtime-confirmed on iPhone 17 Pro Max / iOS 27 Simulator with `UICTContentSizeCategoryAccessibilityXXXL`: the idle timer's task menu wraps “Unassigned” into two lines and cuts off the second line within its dark panel. `Sources/Views/TimerTaskPicker.swift:63-76` applies a single-line/shrink policy to the menu container, but the rendered picker label still overflows. Provide an explicit label with reliable wrapping and sufficient intrinsic height at accessibility sizes; verify long task names and active/next selectors too. Screenshot: `/tmp/pomodorough-review-xxxl-ready.png` (temporary review artifact).

- [x] **P3 — Next-task picker hides its purpose during an active timer.** `Sources/Views/TimerTaskPicker.swift:48,54` hides the “Next focus task” label. Sighted users see the active task followed by another task menu without an explanation that changes apply only to the next focus run; that explanation exists only in an accessibility hint. Show “Next focus task” beside the menu and make the current/next distinction visible. Source-confirmed.

- [x] **P3 — Completed-focus breakdown is hidden behind an unrelated history count.** `Sources/Views/HistoryScreen.swift:66,71` labels its navigation control “N total” visually and “N history entries” for accessibility, but opens a chart restricted to completed focus runs. Users cannot infer the destination, and a history containing only cancelled runs leads from a nonzero count to an empty chart. Use an explicit “Completed focus” / “View focus breakdown” label; show the overall history count separately. Source-confirmed.

- [x] **P2 — Reduce Motion is ignored by timer/control and task transitions.** `Sources/Views/TimerControls.swift:28`, `Sources/Views/TasksScreen.swift:60`, `Sources/Views/TimerScreen.swift:42`, and `Sources/Views/MainContainer.swift:169,183` install animations without consulting `accessibilityReduceMotion`; macOS additionally animates the actual window resize with `setFrame(... animate: true)`. The only existing Reduce Motion check is in NetworkSectionView. Disable spatial/morphing animations or replace them with a non-spatial transition when requested, including the AppKit resize. Source-confirmed; runtime Reduce Motion comparison pending.

- [x] **P2 — Visible timer and task actions disappear as accessibility buttons.** `Sources/Views/TimerControls.swift:34` replaces Start/Pause, Finish, Cancel and Stop sound with one button plus custom actions; `Sources/Views/TaskComposer.swift:49` explicitly hides Add task. Voice Control users cannot target the visible secondary button names through matching accessibility buttons; VoiceOver users must discover the actions rotor even for primary task creation. Expose visible actionable controls individually with matching labels; combine only decorative/read-only content. Existing `UITests/PomodoroughAccessibilityUITests.swift:5` asserts the compressed hierarchy rather than verifying each visible action is independently reachable. Source-confirmed; Voice Control runtime test pending.

- [x] **P2 — System alarm scheduling failure never attempts notification fallback.** `Sources/TimerAlarmScheduler.swift:404,442` propagates an authorized AlarmKit backend's scheduling error before reaching the available notification backend. A backend failure leaves a running timer with no new completion alert even when notifications are authorized. Attempt notification scheduling when AlarmKit scheduling fails, preserving cancellation/duplicate-delivery safeguards, and test with an authorized alarm backend that throws. Source-confirmed; no live OS failure injected.

- [x] **P2 — Task-board daily totals omit unassigned and deleted-task focus runs.** `Sources/Views/TasksScreen.swift:26` sums only visible task rows; `Sources/AppStatePublisher.swift:122,130` skips unassigned runs and returns only current tasks. Repro: finish an unassigned focus, or finish an assigned focus then delete its task; today's headline count/time shows zero or drops despite history retaining that completed work. Compute headline totals from all completed focus history for the day; keep row totals scoped to tasks. Source-confirmed.

- [x] **P2 — Accessible timer layout ignores the largest text settings.** `Sources/Views/AccessibleDialFace.swift:15,25` fixes the countdown at 56 points (shrinkable to 60%) and caps the entire readout at `.accessibility1`. Users selecting Accessibility XXL/XXXL receive the same or smaller readout, phase and status instead of their requested size. Use scaled metrics/text styles and wrapping or scrolling without capping essential timer content. Runtime-confirmed: Accessibility M and XXXL show identical countdown, phase and status sizes on iPhone 17 Pro Max / iOS 27 Simulator. Temporary screenshots: `/tmp/pomodorough-review-axm-ready.png`, `/tmp/pomodorough-review-xxxl-ready.png`.

- [x] **P2 — Task deletion has no confirmation or undo.** `Sources/Views/TasksScreen.swift:43,47` immediately calls `deleteTask`; `Sources/AppModel.swift:588` queues the deletion for sync. An accidental trash tap removes the task and its daily row immediately, with no recovery action or explanation that history remains. Offer Undo backed by a restoring task operation, or confirm deletion. Source-confirmed.

- [x] **P2 — Timer alert status stays stale after returning from Settings.** `Sources/Views/AccountView.swift:212`: authorization refresh runs only in the section's initial `.task` and after the in-app enable action. Repro: deny alerts, open Account → Open Settings, enable permission, return to the still-open Account sheet; it retains “Off” and the settings action. Revoking permission similarly leaves “On”. Refresh when the scene becomes active, including AlarmKit authorization changes. Source-confirmed; device repro pending.

## Validation — current review

- Fixes applied and verified 2026-09-07 (see Fix log below). macOS build + iOS Simulator build succeed. Full `PomodoroughMacTests` suite passed (472s). Targeted iOS UI run passed: `testDialFaceCountdownGrowsWithDynamicType`, `testTaskDeletionCancelKeepsTask`, `testTaskDeletionConfirmRemovesTask` (103s, iPhone 17 Pro Max / iOS 27 sim).
- Reviewed timer controls, task creation/deletion and daily totals, history navigation, account permissions, alarm scheduling and accessibility behavior. Baseline HEAD: `6bc3ee1`; unrelated test/project edits appeared during review and were preserved. This review changes only this backlog.
- iOS Simulator build passed. macOS configured tests passed: 662 tests in 43 suites. iOS configured test run passed: 651 unit/integration tests in 42 suites plus six UI tests. Newly added, concurrent UI test files were not included in this already-started run.
- Code-size audit: zero violations, six documented exceptions. Complexity report completed. `git diff --check -- backlog.md` passed.
- Visually checked idle timer at default text size, Accessibility M and Accessibility XXXL on iPhone 17 Pro Max / iOS 27 Simulator. Two Dynamic Type issues reproduced above. RocketSim accessibility inspection failed with `accessibility_unavailable`; live VoiceOver/Voice Control, real-device permission changes and injected AlarmKit failures remain unverified. Source-only findings explicitly marked.

### Fix log (2026-09-07)

- Contrast: `PomodoroughTheme.signalText` is adaptive (dark `#AC201C` ≈7:1 on white in light, vivid signal in dark); `TaskComposer` ADD TASK uses it.
- Picker clipping + Next label: `TimerTaskPicker` wraps to 3 lines with vertical fixed size at accessibility sizes; visible `Next focus task` caption added above the active-branch picker.
- History label: nonempty list grouped under `N arrivals` header; toolbar shows `Completed focus: N` with a11y label `View focus breakdown` and value `N completed of M runs` (`completedRunCount`).
- Reduce Motion: animations gated on `accessibilityReduceMotion` in `TimerControls`, `TasksScreen`, `TimerScreen`, `MainContainer`, including macOS `setFrame(animate:)`.
- Individual a11y buttons: `TimerControls` exposes primary/Finish/Cancel/Stop-sound/Dismiss as separate buttons; `TaskComposer` Add button unhidden.
- Alarm fallback: `TimerAlarmScheduler` schedule/resume fall through to notifications when AlarmKit throws (with cancel cleanup); covered by `timerAlarmSchedulerFallsBackToNotificationWhenAlarmScheduleFails` + resume variant.
- Daily totals: `AppStatePublisher.dayFocusTotals` / `AppModel.dayFocusTotals` count all completed focus runs for the day including unassigned/deleted; hero uses it, rows unchanged; covered by `dayFocusTotalsIncludeUnassignedAndDeletedTaskRuns`.
- Dial scaling: `AccessibleDialFace` uses `@ScaledMetric` countdown, cap removed; covered by `testDialFaceCountdownGrowsWithDynamicType` (AXL vs XXXL face height; note portrait layout is used below accessibility sizes, so the test compares two accessibility sizes).
- Deletion confirm: `TasksScreen` presents an `.alert` (not `confirmationDialog` — iOS 27 sim exposes no Cancel element for the sheet, verified via hierarchy dump: `sheets=1`, zero `cancel` matches) with Delete task/Cancel; history-preserving message. Covered by `testTaskDeletionCancelKeepsTask` + `testTaskDeletionConfirmRemovesTask` (trash button tapped by trailing-edge coordinate since the row representation folds it).
- Alert refresh: `AccountView` refreshes on `scenePhase == .active`; mapping extracted to pure `timerAlertPresentation(notification:alarm:)` in `Sources/TimerAlertPresentation.swift`, covered by `timerAlertPresentationMapsAuthorizationToAccountControls` (ephemeral case iOS-only).

## Previous review history

Reviews: 2026-09-05 (initial), 2026-09-06 (working tree at `3dc3739` + fixes this session).
Full `PomodoroughMacTests` suite passed; macOS and iOS Simulator builds succeed.

## Resolved

- [x] P1 — Restore published projection + report local save failures (already in source: `AppStatePersistenceCoordinator.swift:488`).
- [x] P1 — iOS foreground timer notifications (already in source: `IOSTimerNotificationCoordinator`).
- [x] P2 — Pull-to-refresh routes through active sync mode (already in source: `AppModel.refreshForPull`).
- [x] P2 — Preserve timer deadlines across alarm queue (already in source: deadline captured before queue).
- [x] P2 — Midnight task-board refresh (already in source: `nextMidnight` task + notifications).
- [x] P2 — AlarmKit resume uses canonical remaining (fixed: `TimerAlarmScheduler.resume` now cancel+schedule; `UnitPositiveTests` updated).
- [x] P2 — History refresh when empty (fixed: `HistoryScreen` empty state is refreshable).
- [x] P2 — Enable timer alerts after skipping onboarding (fixed: Timer Alerts section in `AccountView`).
- [x] P2 — Auto-start guidance copy (fixed: `ServicePatternCard` copy matches behavior).
- [x] P2 — Room join dismissal safety (fixed: `JoinIrohRoomView` uses `.interactiveDismissDisabled(isJoining)` on iOS).

## Previous validation notes

- Regression tests: `SnapshotReplacementRecoveryTests`, `TimerCompletionSchedulingTests`, `IntegrationPositiveTests`, `UnitPositiveTests` (foreground delegate, midnight DST).
- Original baseline: macOS 650 tests/42 suites; iOS sim 638 tests/41 suites + 5 UI tests.
- `PomodoroughHistoryRefreshUITests.testEmptyHistoryExposesRefreshableScrollAndSurvivesPull`: taps Arrivals on a fresh install, asserts the `history.empty-scroll` container exists, performs a real `swipeDown` pull, and asserts the empty state survives. Passes on sim (2026-09-06). Identifier must sit *after* `.accessibilityRepresentation` — inside it the id is swallowed. Remaining gaps: pull gesture on the nonempty list, midnight view invalidation, delayed-join dismissal.
